/*
Numeric data types in C++ are used to handle numeric data like integers, floating-point values, and precision values
1. Integer
-----------
Takes 4 bytes from the memory
int variable_name;

a. Short int
...............
Takes 2 bytes from the memory (16 bits)
short int variable_name;

b. Long int
...............
Memory space of 4 bytes, but can be upto 8 bytes depending on the compiler
long int variable_name;

c. long long int
...................
Memory space of 8 bytes, but can be upto 16 bytes depending on the compiler
long long int variable_name;

d. unsigned int
...................
Used to store only non-negative value, and takes 4 bytes
unsigned int variable_name;

e. unsigned short int
...............
Takes 2 bytes from the memory (16 bits)
unsgined short int variable_name;

f. unsigned long int
...............
Memory space of 4 bytes, but can be upto 8 bytes depending on the compiler
unsigned long int variable_name;

g. unsigned long long int
...................
Memory space of 8 bytes, but can be upto 16 bytes depending on the compiler
unsigned long long int variable_name;

*/

/*
Explicit conversion of numeric data types
In C++, the explicit type conversion is not done automatically, you need to convert the type manually by placing the target data type in the parentheses (). It tells the compilerto convert the value of a variable or an expression to a specific (target) data type
*/