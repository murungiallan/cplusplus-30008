#include <iostream>
using namespace std;

int main() {
	/*
	int number1 = 75;
	int number2{ 60 };
	*/
	bool red{ false };
	bool yellow{ false };
	bool green{ true };
	bool police_stop{ true };

	/*
	* 
	bool result = (number1 < number2); //Expression yielding the condition
	cout << boolalpha << "Result should be: " << result << endl;
	cout << endl;

	cout << "free standing if statement" << endl;

	if (number1 < number2) {
		cout << number1 << " is less than " << number2 << endl;
	}else {
		cout << number1 << " is NOT less than " << number2 << endl;
	}
	*
	*/


	return 0;
}